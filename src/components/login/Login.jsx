import LoginForm from "./LoginForm";

const Login = () => {
  return (
    <div className="container m-auto h-screen w-screen flex justify-center items-center bg-image-login">
      <LoginForm />
    </div>
  );
};

export default Login;
